{
  "name": "backend-json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server db.json --port $PORT --host 0.0.0.0 --middlewares cors.js"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}